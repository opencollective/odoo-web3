// Import RootApp component
import { RootApp } from "./components/RootApp.jsx";

// Initialize app - all dependencies are now imported directly in components
ReactDOM.render(<RootApp />, document.getElementById("root"));
